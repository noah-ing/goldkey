{
  "name": "goldkey-edge-storefront",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "wrangler dev",
    "test": "node --test test/*.test.mjs",
    "check": "npm test && wrangler deploy --dry-run --outdir .wrangler-dist"
  },
  "dependencies": {
    "viem": "2.55.13"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
