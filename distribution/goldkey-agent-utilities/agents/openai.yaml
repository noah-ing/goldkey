interface:
  display_name: "GoldKey Action Gate"
  short_description: "Preflight agent actions with a $0.01 decision gate"
  default_prompt: "Use $goldkey-agent-utilities to probe or run the $0.01 Action Gate before a proposed agent action."
